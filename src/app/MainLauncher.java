package app;

import config.AppConfig;

/**
 * Main entry point for the Academic Feedback System
 */
public class MainLauncher {
    
    public static void main(String[] args) {
        System.out.println("Starting " + AppConfig.APP_NAME + " v" + AppConfig.APP_VERSION);
        // TODO: Initialize and launch the main GUI frame
        // LoginFrame loginFrame = new LoginFrame();
        // loginFrame.setVisible(true);
    }
}
